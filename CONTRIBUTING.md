# Contributing

Bug reports, reproducibility issues, and focused pull requests are welcome. Please open an issue describing the physical or numerical problem before large changes. New model terms should include: (1) a documented equation or algorithm, (2) a minimal test, and (3) a reproducible example or regression target.

Development setup: `pip install -e '.[dev]'`, then run `pytest`.
