# Minimal diffusion-tensor example

```bash
hopping3d examples/diffusion_tensor/params.json
```

This example performs unbiased periodic KMC and writes the full 3x3 diffusion tensor, principal diffusivities, and principal axes to `results/`.
